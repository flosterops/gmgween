import styled from 'styled-components';
import { colors } from 'theme/colors.theme';

export const StyledAmountsList = styled.ul`
  columns: 2;
  list-style: none;

  a {
    color: ${colors.orange};
  }
`;
